mixin practice({ name, title, date, organization, text })
  li(class=`${name}__item`)
    p(class=`${name}__item-title`)
      span(class=`${name}__item-name`)= title
      =` // ${date}`
    span(class=`${name}__item-organization`)= organization
    p(class=`${name}__item-text`)= text