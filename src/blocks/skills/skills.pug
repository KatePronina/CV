include ../header/header.pug
include ../skill/skill.pug

mixin skills({ name, maxValue, items })
  section.skills
    +header({ name })
    ul.skills__list
      each item in items
        +skill({ maxValue, value: item.value, title: item.title })
