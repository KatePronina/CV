mixin contact(name, ...contacts)
    section.contact
        +header(name)
        table.contact__items
            each contact in contacts
                tr.contact__item
                    td.contact__name= contact.name
                    td.contact__value= contact.value
